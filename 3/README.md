To run the code, simply run python3 process.py from the root directory. Currently, the code for part A is commented out so that the code doesn't take an exceptional amount of time to run. In the code are multiple functions, and if the file is run, the entire script for part B will run and output different images. Each section is labeled with a comment. 